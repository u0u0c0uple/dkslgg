<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dkslgg.record.model.dao.AccountDao">
    <select id="selectAccount" parameterType="String" resultType="AccountVo">
        SELECT * FROM ACCOUNT WHERE game_name = #{gameName} AND tag_line = #{tagLine};
    </select>
    <select id="existAccount" parameterType="List" resultType="String">
        SELECT puuid FROM ACCOUNT
        WHERE puuid IN (<foreach collection="puuidList" item="item" separator=","> #{item} </foreach>);
    </select>
    <insert id="insertAccount" parameterType="AccountVo">
        INSERT INTO ACCOUNT(puuid, game_name, tag_line, created_at, updated_at)
        VALUES (#{puuid}, #{gameName}, #{tagLine}, NOW(), NOW())
        ON DUPLICATE KEY UPDATE game_name = #{gameName}, tag_line = #{tagLine}, updated_at = NOW();
    </insert>
</mapper>